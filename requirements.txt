sentence-transformers>=3.4
datasets>=3.0
numpy>=1.26
httpx>=0.27
pytest>=8.0
streamlit>=1.40
streamlit-audio-recorder>=0.2
faster-whisper>=1.0

# Optional lighter offline fallback for Linux/Windows only (no macOS arm64
# wheel): pip install vosk  →  VRAG_STT_FALLBACKS=vosk,faster-whisper